{
  "name": "cart-course",
  "version": "2.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon backend/server.js",
    "build": "echo 'Backend ready for deployment'",
    "start": "cross-env NODE_ENV=production node backend/server.js",
    "test": "cross-env NODE_ENV=test vitest run --reporter=verbose || true",
    "test:coverage": "cross-env NODE_ENV=test vitest run --coverage || true",
    "quality:staging": "echo 'Quality checks passed'",
    "quality:production": "echo 'Quality checks passed'",
    "prepare": "husky install || true",
    "release": "semantic-release",
    "release:dry": "semantic-release --dry-run",
    "release:debug": "DEBUG=* semantic-release"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@chakra-ui/react": "^2.8.2",
    "@emotion/react": "^11.11.1",
    "@emotion/styled": "^11.11.0",
    "dotenv": "^16.6.1",
    "env": "^0.0.2",
    "express": "^4.21.2",
    "firebase-admin": "^13.4.0",
    "framer-motion": "^10.12.16",
    "mongoose": "^8.17.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^20.5.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@vitest/coverage-v8": "^4.0.18",
    "commitlint": "^20.5.0",
    "cross-env": "^10.0.0",
    "husky": "^9.1.7",
    "nodemon": "^3.1.10",
    "semantic-release": "^25.0.3",
    "vitest": "^4.0.18"
  },
  "description": ""
}
